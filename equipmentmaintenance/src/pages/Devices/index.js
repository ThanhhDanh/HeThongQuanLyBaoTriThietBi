import TypeDeices, { TypeDeicesItem } from '~/common/TypeDevices';

function Devices() {
    return (
        <TypeDeices>
            <TypeDeicesItem />
        </TypeDeices>
    );
}

export default Devices;
